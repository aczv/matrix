version: '3'

services:
  web:
    build: .
    volumes:
      - ./.env:/app/.env:ro
    networks:
      - prod
    ports:
      - "${PORT}:8000"
    restart: always

networks:
  prod:
    external: true
